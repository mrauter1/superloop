<autoloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T19:45:06.699268+00:00" />
Default Claude support must use the local claude CLI only; do not introduce any Anthropic or Claude SDK integration in this slice.
Keep the implementation localized to src/autoloop/main.py with a small provider-neutral execution seam; do not introduce a new provider package unless later work justifies it.
Support the new nested provider config shape, but continue loading legacy flat Codex provider keys and legacy session files that only contain thread_id.
Resume must fail fast when the stored session provider and the currently selected provider differ instead of silently starting a mismatched conversation.
Default Claude mode must preserve the user’s existing Claude Code environment, so v1 must not pass --bare, system-prompt replacement flags, tool restriction flags, or --max-turns by default.
Strict-mode/tool restriction work and stream-json telemetry are explicitly deferred until after inherited-environment Claude validation shows they are needed.
<autoloop-decisions-header version="1" block_seq="2" owner="implementer" phase_id="provider-runtime-refactor" pair="implement" turn_seq="1" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T19:49:26.597265+00:00" />
Legacy flat provider.model/provider.model_effort keys remain a codex-only alias, override same-file provider.codex values when present, and are rejected when provider.name is claude to avoid ambiguous cross-provider overrides.
execute_pair_cycles keeps backward-compatible codex_command support for current tests and callers, but resolves it immediately into the new ProviderRuntime seam so future providers can reuse the same phase execution path without another loop refactor.
For session migration, default_provider only applies when the session file is missing; any existing payload without an explicit provider field must normalize to provider=codex even if its stored thread_id/session_id is null.
<autoloop-decisions-header version="1" block_seq="3" owner="test_author" phase_id="provider-runtime-refactor" pair="test" turn_seq="1" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T19:59:54.000918+00:00" />
Session-loader coverage is intentionally split between existing payloads and missing files so future changes cannot regress the legacy codex-normalization rule by conflating it with new-session default_provider seeding.
<autoloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="claude-cli-adapter" pair="implement" turn_seq="1" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T20:01:12.859898+00:00" />
Claude phase turns keep the run/request/artifact preamble in the `claude -p` prompt body, but move the pair template plus Autoloop loop-control guardrails into a per-turn `--append-system-prompt-file` so Claude Code defaults remain intact.
Successful Claude turns persist provider_metadata from the returned JSON payload in session state, while malformed JSON and non-zero Claude exits are logged as provider_failure raw-log entries before the run fatals so they cannot be misclassified as loop-control parse failures.
<autoloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="claude-cli-adapter" pair="implement" turn_seq="2" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T20:09:11.857268+00:00" />
ProviderExecutionError handling is now centralized so both normal phase turns and `full_auto_answers` auto-answer turns append `provider_failure` entries to task/run raw logs before fatal exit.
Session state now persists Claude-specific explicit `model_override` and `effort_override` values alongside `provider_metadata`; leave them null for providers that did not receive explicit Claude overrides in this slice.
<autoloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="claude-cli-adapter" pair="test" turn_seq="1" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T20:12:28.222511+00:00" />
Keep the Claude `full_auto_answers` path covered at two levels: a helper-level failure test for raw-log/fatal handling and a loop-level `execute_pair_cycles()` test that verifies the auto-answer context wiring without requiring a real Claude subprocess.
<autoloop-decisions-header version="1" block_seq="7" owner="implementer" phase_id="validation-and-operator-surface" pair="implement" turn_seq="1" run_id="run-20260324T194506Z-445f9245" ts="2026-03-24T20:14:51.609103+00:00" />
Operator-facing messaging for this slice includes README, installer output, and CLI help text; they must all describe provider-specific prerequisites and the provider-aware `full_auto_answers` path consistently.
The explicit Claude live-validation checklist is recorded in `docs/claude_rollout_checklist.md` so rollout guidance ships with the repository instead of staying implicit in phase notes.
