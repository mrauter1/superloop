<superloop-decisions-header version="1" block_seq="1" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260323T004953Z-6d60168d" ts="2026-03-23T00:49:53.891303+00:00" />
Treat this task as a greenfield implementation: the PRD is the product contract and there is no existing application code to preserve beyond the planning artifacts.
The initial database migration must include the full Stage 1 schema and constraints, including the partial unique index for one active ai_run per ticket, rather than incrementally adding critical invariants later.
Session auth must be implemented as a custom PostgreSQL-backed opaque-token system; Starlette SessionMiddleware or any other signed-cookie session store must not hold primary auth state.
Shared transactional helpers must own status-history writes, updated_at bumps, ticket view updates, and enqueue/requeue transitions so requester routes, ops routes, and worker actions cannot drift.
Worker publication logic must use the same shared fingerprint rules for skip checks and stale-run suppression, and successful non-superseded runs must publish exactly one internal AI note before any action-specific public output or draft mutation.
<superloop-decisions-header version="1" block_seq="2" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260323T005204Z-055db9ce" ts="2026-03-23T00:52:04.836359+00:00" />
Multipart parsing must enforce the PRD limits at parse time with explicit request.form max_files and max_part_size settings; post-parse validation alone is not sufficient.
The worker/Codex boundary is contract-sensitive: final.json written by --output-last-message is the only authoritative model result, while JSONL/stderr artifacts remain diagnostic only.
Workspace bootstrap must write the exact AGENTS.md and .agents/skills/stage1-triage/SKILL.md content from the PRD at those exact paths and verify the app/manuals mounts instead of inferring alternative layouts.
Shared transaction-scoped ticket mutation services should be the only place that combine state changes, status history, updated_at bumps, ticket view updates, and enqueue/requeue side effects across requester routes, ops routes, and worker actions.
<superloop-decisions-header version="1" block_seq="3" owner="planner" phase_id="task-global" pair="plan" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T00:54:37.625336+00:00" />
Because the repo is greenfield aside from the PRD and superloop metadata, the Stage 1 route set, CLI command names, environment variables, schema semantics, and filesystem paths should be treated as first-class contracts rather than implementation details.
Phase completion should be gated by runnable invariants, not just code presence: workspace bootstrap, custom-session auth, requester read semantics, shared ops mutations, worker publication order, and end-to-end acceptance validation each need explicit proof before the next slice is considered stable.
Internal notes may inform AI context but must stay out of automatic-trigger and publication fingerprints; only a manual rerun should cause newly added internal notes to influence a later AI run.
<superloop-decisions-header version="1" block_seq="4" owner="implementer" phase_id="foundation-persistence" pair="implement" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T00:56:43.705555+00:00" />
The initial migration uses explicit SQL for descending indexes and the partial unique ai_runs index so the PostgreSQL contract is emitted exactly instead of depending on Alembic helper behavior for expression indexes.
Script entry points prepend the repository root to sys.path so the required `python scripts/...` workflow works from a clean checkout without packaging the project first.
This phase intentionally stops at scaffolded web/worker modules plus shared queue/fingerprint/session foundations; requester/ops flows and Codex publication logic remain deferred to their later scoped phases.
<superloop-decisions-header version="1" block_seq="5" owner="implementer" phase_id="foundation-persistence" pair="implement" turn_seq="2" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:06:10.302614+00:00" />
Queue-safe pending ai_run creation now relies on the database partial unique index inside a nested transaction/savepoint and normalizes that specific race to `None` instead of depending on a pre-check query.
Shared status-history recording accepts an explicit `from_status_override`, so the required initial `null -> new` history row can stay on the shared helper path instead of forcing later phases to bypass it.
Alembic bootstrap now reads `DATABASE_URL` directly through a DB-only config helper so schema creation is not coupled to unrelated Codex or workspace environment variables.
<superloop-decisions-header version="1" block_seq="6" owner="test_author" phase_id="foundation-persistence" pair="test" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:08:20.448745+00:00" />
Foundation-persistence tests cover ai_run conflict normalization with deterministic fake session/savepoint objects instead of a live PostgreSQL database so the helper’s branching logic is isolated from infrastructure setup.
Workspace bootstrap tests use a disposable git-backed temp directory and exact-file-content assertions to lock the PRD-controlled `AGENTS.md` and `SKILL.md` contracts without touching real mounts.
<superloop-decisions-header version="1" block_seq="7" owner="test_author" phase_id="foundation-persistence" pair="test" turn_seq="2" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:11:08.971262+00:00" />
Foundation-persistence regression tests now assert that the ai_run helper enters a nested transaction/savepoint, because returning `None` on a uniqueness conflict is not sufficient if the outer session can still be poisoned by a failed flush.
Alembic bootstrap and migration contract coverage use source-level assertions against `shared/migrations/env.py` and the initial migration file so the DB-only loader wiring and required schema/index markers are protected without needing a live database.
SQLAlchemy-dependent helper tests import those modules lazily and skip only when the dependency is absent, so source/bootstrap contract tests still run in minimal environments instead of failing the entire suite during collection.
<superloop-decisions-header version="1" block_seq="8" owner="implementer" phase_id="auth-requester" pair="implement" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:13:18.037390+00:00" />
Requester ticket create/reply/resolve now go through shared.ticketing helpers so status-history rows, updated_at bumps, ticket_views writes, and enqueue/requeue behavior stay on one transaction-scoped path instead of being reimplemented in routes.
Authenticated requester GET handlers explicitly commit after auth/view updates because the shared DB dependency does not auto-commit; this preserves session `last_seen_at` and the PRD-required ticket detail read tracking on successful GETs.
Requester-phase tests use repo-root path bootstrapping in `tests/conftest.py` and skip dependency-bound cases when FastAPI/argon2/SQLAlchemy are unavailable, so source-level contract checks still run in minimal environments without hiding real helper regressions when the stack is installed.
Multipart parsing now adds a small fixed slack margin above `MAX_IMAGE_BYTES` for `max_part_size` so valid 5 MiB uploads survive multipart header overhead while the explicit post-parse byte check still enforces the true file-size cap.
Requester-thread author labels are now derived from explicit `author_type` mapping in shared UI helpers and serialized route data, so future public Dev/TI or system messages cannot be misattributed to the requester by template fallthrough.
<superloop-decisions-header version="1" block_seq="9" owner="implementer" phase_id="ops-workflow" pair="implement" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:28:08.251776+00:00" />
`/ops` is implemented as the filtered queue/list page and `/ops/board` as the grouped status board so the phase satisfies the PRD's separate list and board surfaces without creating extra routes beyond the contract.
Ops draft approval publishes the saved draft as an `ai` public message with source `ai_draft_published`, while the human reviewer remains the actor recorded on the draft review fields and any resulting status transition.
Ops AI-analysis panels read `relevant_paths`, summaries, and similar structured fields from the canonical run artifact at `ai_runs.final_output_path` when present, avoiding a second persistence model before the worker phase owns publication storage decisions.
<superloop-decisions-header version="1" block_seq="10" owner="test_author" phase_id="ops-workflow" pair="test" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:38:37.574103+00:00" />
Ops-workflow tests intentionally combine lazy-import helper/route tests with source-level template assertions so required filters, AI panels, and public/internal separation still stay covered when FastAPI/SQLAlchemy/argon2 are unavailable in the test environment.
Manual-rerun coverage explicitly exercises both the active-run requeue branch and the idle-run `waiting_on_dev_ti -> ai_triage` branch, because the ops phase contract depends on preserving those shared queue/state invariants.
<superloop-decisions-header version="1" block_seq="11" owner="test_author" phase_id="ops-workflow" pair="test" turn_seq="2" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:42:01.774558+00:00" />
Ops-workflow requester-authorization coverage now explicitly spans `/ops`, `/ops/board`, and `/ops/tickets/{reference}` so AC-3 is protected across the full GET surface instead of only the queue route.
Idle manual-rerun coverage uses a `create_pending_ai_run` stub that accepts positional and keyword arguments, matching the production helper call shape so the test still executes once dependency-backed runs stop skipping.
<superloop-decisions-header version="1" block_seq="12" owner="implementer" phase_id="ai-worker" pair="implement" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:43:27.380394+00:00" />
Because the fingerprint contract includes `ticket.status`, the worker now moves tickets into `ai_triage` before computing `ai_runs.input_hash`; otherwise a normal `new -> ai_triage` transition would falsely trip stale-run suppression on every fresh run.
Deferred requeue creation now flushes the just-finished run status before checking for active runs, because the shared SQLAlchemy session is configured with `autoflush=False` and would otherwise still see the current run as active in the database query path.
<superloop-decisions-header version="1" block_seq="13" owner="test_author" phase_id="ai-worker" pair="test" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T01:55:48.469240+00:00" />
Ai-worker regression coverage stays unit-level and deterministic by monkeypatching session scopes, Codex execution, and publication helpers, because the phase risks are in worker lifecycle branching and contract enforcement rather than live external process behavior.
Heartbeat coverage uses a stop-event-driven `heartbeat_loop` call with a zero-second interval to prove decoupled heartbeat emission without introducing timing-based flakes.
<superloop-decisions-header version="1" block_seq="14" owner="implementer" phase_id="hardening-validation" pair="implement" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T02:00:28.371410+00:00" />
Readiness now checks the full workspace contract surface needed by Stage 1 operations: workspace dir, runs dir, app/manual mounts, AGENTS.md, and .agents/skills/stage1-triage/SKILL.md, and returns an explicit 503 JSON payload instead of surfacing raw exceptions.
Structured JSON logging is centralized in shared/logging.py so the web request middleware and worker lifecycle/heartbeat logs share the same timestamp/service/level/event shape without introducing a broader logging framework refactor.
Hardening validation proof is captured with repo-local artifacts rather than environment-coupled end-to-end automation: .env.example documents the required variables, README contains the acceptance checklist, and tests lock readiness, rendering sanitization, workspace-contract checks, and request logging behavior.
The bootstrap/web/worker validation gap is closed by adding deterministic `--check` modes to `scripts/run_web.py` and `scripts/run_worker.py`, which lets subprocess-based tests exercise the real entrypoints with temp local environment variables without hanging on long-running processes.
Worker skip handling now checks `last_processed_hash` before forcing `ai_triage`, then recomputes the run fingerprint after any required status transition; this preserves the earlier stale-run-suppression fix while restoring the PRD rule that skipped runs do not change ticket status.
Dependency-backed route tests now run as part of the hardening suite, so `httpx` is a declared test/runtime harness dependency rather than an implicit local prerequisite for FastAPI `TestClient`.
<superloop-decisions-header version="1" block_seq="15" owner="test_author" phase_id="hardening-validation" pair="test" turn_seq="1" run_id="run-20260323T005437Z-5ff4890a" ts="2026-03-23T02:08:57.774420+00:00" />
Hardening-validation tests now treat the new script `--check` modes as the authoritative acceptance-validation surface: subprocess smoke coverage exercises both the green path after bootstrap and the failure path before bootstrap using temp local env vars, rather than asserting a specific deployment shell workflow.
The worker script failure-path assertion is intentionally path-order agnostic and only checks the stable contract that an unbootstrapped workspace causes a missing-required-path failure, because readiness may fail first on `runs/`, `AGENTS.md`, or the skill file depending on bootstrap state.
