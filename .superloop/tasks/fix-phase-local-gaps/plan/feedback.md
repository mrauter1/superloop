# Plan ↔ Plan Verifier Feedback

- 2026-03-21: Replaced the placeholder plan with one explicit implementation phase covering strict session routing, correct fresh bootstrap extraction, explicit bootstrap size capping, and run-artifact hygiene. Documented the exact helper contracts to change, the preferred `.superloop/tasks/*/runs/*` hygiene strategy, and the focused/full test expectations so the implementation pair can work without reopening scope.
- PLAN-001 | blocking | The repository-hygiene plan is not yet implementable against the current runtime staging path. Evidence: [`/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/plan.md`](/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/plan.md) recommends ignoring `.superloop/tasks/*/runs/*`, but [`/workspace/superloop/superloop.py`](/workspace/superloop/superloop.py) still includes `runs/` in `tracked_superloop_paths(...)` and then stages/commits `task_scoped_paths` through `commit_tracked_changes(...)` and `try_commit_tracked_changes(...)` during normal execution. Concrete failure scenario: the implementation pair follows the plan, adds `.gitignore`, and still gets run-artifact diffs/commits because the runtime explicitly stages tracked `runs/` paths, especially for already tracked historical run files. Minimal correction: update the plan and `phase_plan.yaml` so the hygiene work explicitly covers the runtime git-staging/commit path for task runs, or explicitly defines another mechanism that satisfies objective D despite existing tracked `runs/` history.
- 2026-03-21: Updated the plan and `phase_plan.yaml` to address `PLAN-001`. The hygiene milestone now explicitly requires changing the runtime tracked-path/git-staging behavior for task `runs/` paths and treats `.gitignore` only as optional secondary protection, which makes objective D implementable against the current `tracked_superloop_paths(...)` and commit-helper flow.
- PLAN-002 | non-blocking | Re-review found no remaining blocking issues. The revised plan now addresses the prior hygiene gap in the affected sections of [`/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/plan.md`](/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/plan.md) and [`/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/phase_plan.yaml`](/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/phase_plan.yaml) by making runtime tracked-path/git-staging changes part of Milestone 4, the repository-hygiene contract, risk controls, and acceptance criterion `AC-8`.
- 2026-03-21: Revalidated the plan against the current `superloop.py` and focused tests for this run. Tightened the plan/YAML so fresh bootstrap helpers must receive explicit ordered prior-phase context, and updated the machine-readable `request_snapshot_ref` to the current authoritative run request path.
- PLAN-003 | non-blocking | Verifier re-review after the latest producer update found no blocking issues. The added explicit prior-phase helper boundary in [`/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/plan.md`](/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/plan.md) and acceptance criterion `AC-4a` in [`/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/phase_plan.yaml`](/workspace/superloop/.superloop/tasks/fix-phase-local-gaps/plan/phase_plan.yaml) make the bootstrap refactor more deterministic and testable without expanding scope, and the current-run `request_snapshot_ref` now matches the authoritative request for this verification turn.
- 2026-03-21: Revalidated the plan against the current working tree for run `run-20260321T180415Z-06b0bd69` and rewrote the plan/YAML to match reality. The strict scoped-session and fresh-bootstrap contract is already present in `superloop.py`, tests, and README, so the plan now treats that behavior as the baseline to preserve and makes the remaining diff-based run-artifact commit gap the primary implementation target. The machine-readable `request_snapshot_ref` now points at the current run's authoritative request file.
- PLAN-004 | non-blocking | Current verifier pass found no blocking issues in the rewritten plan. Evidence review against [`/workspace/superloop/superloop.py`](/workspace/superloop/superloop.py) confirms the plan now matches the live code state: strict session/bootstrap behavior is already present, and the remaining concrete implementation target is the snapshot-delta commit path gap where `run_codex_phase(...)` writes run artifacts that can still appear in `producer_delta` / `verifier_delta`. The single-phase decomposition, acceptance criteria, and rollback/risk controls are coherent and implementation-ready for this scope.
- 2026-03-21: Revalidated the plan against the current run `run-20260321T180746Z-6bd1961f`. Updated the machine-readable `request_snapshot_ref` to this run’s authoritative request file and tightened the narrative/risk notes to call out the currently dirty generated run-output directories and user-modified `.gitignore`, so the implementation pair preserves the requested scope instead of absorbing unrelated runtime artifacts.
- PLAN-005 | non-blocking | Current verifier pass found no blocking issues. Evidence review against [`/workspace/superloop/superloop.py`](/workspace/superloop/superloop.py), [`/workspace/superloop/tests/test_phase_local_behavior.py`](/workspace/superloop/tests/test_phase_local_behavior.py), and [`/workspace/superloop/tests/test_superloop_observability.py`](/workspace/superloop/tests/test_superloop_observability.py) confirms the plan still matches the live repository state: strict session/bootstrap behavior is already implemented and covered by focused regressions, while the remaining concrete implementation target is the snapshot-delta commit-path filtering gap around `producer_delta` / `verifier_delta`. The single-phase `phase_plan.yaml` remains coherent, acceptance criteria are implementation-verifiable, and the added scope note about dirty generated run outputs is appropriate given the current worktree.
